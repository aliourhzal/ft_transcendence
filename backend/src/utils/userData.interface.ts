
// the type of the user data provided by the 42 oauth
interface UserData {
    intra_id: number,
    email: string,
    login: string,
    firstName: string,
    lastName: string,
    profilePic: string,
    wallet: number,
    level: number,
    grade: string,
    access_token: string
}